<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      This is your app's "night" theme.
      It MUST match the definitions in the other themes.xml file.
    -->
    <style name="Theme.Onesec" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your night theme here if you want -->
        <!--
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        -->
    </style>

    <!--
      We also add the transparent theme definition here
      to be complete, even though it's not strictly needed.
    -->
    <style name="Theme.Onesec.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item> <!-- Dims the app behind it -->
    </style>
</resources>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      1. This is your app's main theme.
    -->
    <style name="Theme.Onesec" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your main theme here if you want -->
    </style>

    <!--
      2. This is the special transparent theme for our PopupActivity.
    -->
    <style name="Theme.Onesec.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!--
      3. NEW: Custom Theme for the Search Bar Text
    -->
    <style name="SearchViewTheme" parent="ThemeOverlay.AppCompat.Light">
        <!-- This changes the color of the text you TYPE (currently Black) -->
        <item name="android:editTextColor">#000000</item>

        <!-- This changes the color of the HINT "Search apps..." (currently Gray) -->
        <item name="android:textColorHint">#808080</item>
    </style>
</resources>